import Categories from "@/elements/categories";
import SearchBar from "@/elements/searchBar";

function Home() {
  return (
    <div>
      <SearchBar />
      <Categories />
    </div>
  );
}

export default Home;
